version: '3.8'

services:
  factuality-ai-assessor:
    build: .
    container_name: factuality_ai_assessor
    ports:
      - "8501:8501"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
      - GEMINI_MODEL=${GEMINI_MODEL:-gemini-3.6-flash}
    restart: unless-stopped
